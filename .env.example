# Knitting-event signup app — local development secrets.
# Generate real random values with:  deno task generate-tokens

SIGNUP_TOKEN=
ADMIN_TOKEN=
SESSION_SECRET=

# Optional overrides (sensible defaults exist for all of these):
# EVENT_NAME=Strikkefest
# EVENT_YEAR=2026
# PORT=8000
# KV_PATH=data/app.kv
